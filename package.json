{
    "name": "gearpit-root",
    "private": true,
    "workspaces": [
        "apps/gearpit-web"
    ],
    "scripts": {
        "db:seed": "DATABASE_URL=\"host=localhost user=gearpit password=password dbname=gearpit port=5432 sslmode=disable TimeZone=Asia/Tokyo\" go run apps/gearpit-core/cmd/seeder/main.go",
        "test:e2e:local": "npm run db:seed && docker run --rm --ipc=host --network host -v \"$(pwd)\":/work -w /work/apps/e2e mcr.microsoft.com/playwright:v1.40.0-jammy /bin/bash -c \"npm install && CI=true npx playwright test\""
    }
}